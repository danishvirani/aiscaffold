---
description: Run {{name}}'s {{command}} command
---

Run the `{{command}}` command from the {{name}} CLI and report the result.

```bash
{{name}} {{command}}
```

This is the scaffolded example command. Replace this file's body — and the
matching `{{command}}` handler in your CLI source — with your tool's real
behavior. Add a new slash command by dropping another `.md` file in this
directory and wiring the matching command in the CLI.
