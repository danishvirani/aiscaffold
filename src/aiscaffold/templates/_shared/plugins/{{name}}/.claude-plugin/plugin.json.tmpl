{
  "name": "{{name}}",
  "description": "{{description}}",
  "version": "0.0.1",
  "author": { "name": "{{author}}" }
{{#if with_mcp}}
  ,
  "mcpServers": {
    "{{name}}": { "command": "{{name}}", "args": ["mcp"] }
  }
{{/if}}
}
